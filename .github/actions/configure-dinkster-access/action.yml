name: Configure Dinkster dependency access
description: Configure read-only SSH access without writing a private key to disk
inputs:
  deploy-key:
    description: Read-only Dinkster deploy key
    required: true
runs:
  using: node20
  main: agent.cjs
  post: agent.cjs
  post-if: always()
